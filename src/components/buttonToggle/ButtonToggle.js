// import { selectIsAutoSorted, setIsAutoSorted } from "@/app/features/phrases/phrasesSlice"
// import Toggle from "../toggle/Toggle"
// import styles from './buttontoggle.module.css'

export default function ButtonToggle({
  ariaDescribedby
}) {
  // const on = useAppSelector(selectIsAutoSorted)
  // const dispatch = useAppDispatch()

  // function handleClick() {
  //   dispatch(setIsAutoSorted())
  // }
  
  return ( 
    // <button className={styles.toggle} type="button" aria-pressed={on} onClick={handleClick} aria-describedby={ariaDescribedby}>
    //   Автоматическая сортировка карточек 
    //   <span data-testid="buttonToggle-status" className="visually-hidden">
    //     {on ? 'Включено': 'Выключено'}
    //   </span>
    //   <Toggle isOn={on}/>
    // </button>
    <button>Temp</button>
  )
}