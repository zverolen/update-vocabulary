// import CardsItem from "../cardsItem/CardItem"
// import { useState } from "react"

export default function CardsContainer() {
  // const phrases = []
  
  // const uncheckedPhrases = phrases2.filter(phrase => phrase.selfCheckStatus === 'unset')
  // const correctPhrases = phrases2.filter(phrase => phrase.selfCheckStatus === 'correct')
  // const wrongPhrases = phrases2.filter(phrase => phrase.selfCheckStatus === 'wrong')

  // const [ phrases3, setPhrases3 ] = useState(phrases.map(phrase => {
  //   return {...phrase, selfCheckStatus: 'unset'} 
  //     })
  // )
 
  return (
    <div>
      Cards
      {/* {!isAutoSortOn && 
        phrases2.map((phrase) => (<Card 
          key={phrase.id} 
          data={phrase} 
          getId={()=>{}}
          selfCheckStatus={phrase.selfCheckStatus}
        />))
      }

      {isAutoSortOn && uncheckedPhrases.map((phrase) => (<Card 
        key={phrase.id} 
        data={phrase} 
        getId={()=>{}}
        selfCheckStatus={phrase.selfCheckStatus}
      />))}
      
      {isAutoSortOn && correctPhrases.map((phrase) => (<Card 
        key={phrase.id} 
        data={phrase} 
        getId={()=>{}}
        selfCheckStatus={phrase.selfCheckStatus}
      />))}

      {isAutoSortOn && wrongPhrases.map((phrase) => (<Card 
        key={phrase.id} 
        data={phrase} 
        getId={()=>{}}
        selfCheckStatus={phrase.selfCheckStatus}
      />))} */}
    </div>
  )
}